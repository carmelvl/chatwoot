{
  "name": "kita-bridges",
  "private": true,
  "version": "0.1.0",
  "description": "Slack / Microsoft Teams / Viber <-> Chatwoot API-channel bridge",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --disable-warning=ExperimentalWarning --test --test-reporter=spec \"test/**/*.test.ts\""
  }
}
